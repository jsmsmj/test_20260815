# sample-game (test_20260815)

スマホ縦持ち専用(横持ち非対応)、PCブラウザでも開けるWebゲーム。

## 起動方法

```
npm start
```

`http://localhost:8080/` で確認。静的ファイルを都度読み直すので、HTML/CSS/JSを編集してもサーバー再起動は不要(ブラウザのリロードのみでよい)。

## 画面設計の方針(確定事項)

- 基準解像度は `main.js` の `BASE_WIDTH` / `BASE_HEIGHT`。実機のブラウザ表示領域(アドレスバー等を除いた可視領域)の縦横比に近い値を使う。
- 拡縮は **contain方式**(`scale = Math.min(innerWidth/BASE_WIDTH, innerHeight/BASE_HEIGHT)`)。**canvasが見切れる(クロップされる)ことは禁止**。縦横比を保ったまま常に全体が画面内に収まるように縮小する。ストレッチはしない。
- 余り(レターボックス)は許容する。`#game-container` の配置は横`center`・縦`flex-start`(上寄せ)。画面下端に余白が出ることがあるため、重要なUI・操作要素は下端ギリギリには置かない。

## 作業上の注意

- **ユーザーの明示的な許可なくファイルを編集しないこと。** レイアウト・拡縮方式・UX仕様に関わる変更は、内容を文章で提示し明確な同意を得てから実装する。方向性を示す発言だけでは実装許可とみなさない。
